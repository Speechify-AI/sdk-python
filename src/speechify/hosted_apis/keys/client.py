# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.pagination import AsyncPager, SyncPager
from ...core.request_options import RequestOptions
from ...types.hosted_api_key import HostedApiKey
from ...types.list_hosted_api_keys_response import ListHostedApiKeysResponse
from .raw_client import AsyncRawKeysClient, RawKeysClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class KeysClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawKeysClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawKeysClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawKeysClient
        """
        return self._raw_client

    def list_keys(
        self,
        api_id: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[HostedApiKey, ListHostedApiKeysResponse]:
        """
        List a hosted API's consumer keys, newest first, revoked ones included.
        Dark launch: requires the `hosted_apis_access` entitlement (402 `hosted_apis_not_in_plan` otherwise).

        Parameters
        ----------
        api_id : str
            Hosted API id (prefixed external id, `api_...`).

        cursor : typing.Optional[str]
            Opaque pagination cursor from a previous response.

        limit : typing.Optional[int]
            Max items per page (default 50, max 200).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[HostedApiKey, ListHostedApiKeysResponse]
            OK

        Examples
        --------
        from speechify import Speechify

        client = Speechify(
            "2026-09-30",
            token="YOUR_TOKEN",
        )
        response = client.hosted_apis.keys.list_keys(
            api_id="api_01jqr8x9zg5k2m3n4p5q6r7s8t",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list_keys(api_id, cursor=cursor, limit=limit, request_options=request_options)

    def create_key(
        self,
        api_id: str,
        *,
        name: str,
        idempotency_key: typing.Optional[str] = None,
        rate_per_minute: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HostedApiKey:
        """
        Mint a consumer key (`ck_...`) for the API's own callers. The plaintext
        `secret` is present in this response only; every later read shows the
        masked `key_hint`.
        Dark launch: requires the `hosted_apis_access` entitlement (402 `hosted_apis_not_in_plan` otherwise).

        Parameters
        ----------
        api_id : str
            Hosted API id (prefixed external id, `api_...`).

        name : str

        idempotency_key : typing.Optional[str]
            A client-generated key (an opaque string, max 255 chars) that makes a
            side-effect POST safe to retry: the server runs the operation exactly
            once and replays the first response (its status and body) for 24 hours.
            Reusing a key with a different request body, or while the first request
            is still in flight, returns `409 idempotency_conflict`. A replayed
            response carries the `Idempotent-Replayed: true` header.

        rate_per_minute : typing.Optional[int]
            Requests per minute; 60 when omitted, 0 for unlimited.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HostedApiKey
            The created key, with its one-time secret.

        Examples
        --------
        from speechify import Speechify

        client = Speechify(
            "2026-09-30",
            token="YOUR_TOKEN",
        )
        client.hosted_apis.keys.create_key(
            api_id="api_01jqr8x9zg5k2m3n4p5q6r7s8t",
            idempotency_key="a1b2c3d4-5e6f-7a8b-9c0d-1e2f3a4b5c6d",
            name="name",
        )
        """
        _response = self._raw_client.create_key(
            api_id,
            name=name,
            idempotency_key=idempotency_key,
            rate_per_minute=rate_per_minute,
            request_options=request_options,
        )
        return _response.data

    def revoke_key(
        self, api_id: str, consumer_key_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HostedApiKey:
        """
        Revoke a consumer key; idempotent. Requests carrying it are refused from now on.
        Dark launch: requires the `hosted_apis_access` entitlement (402 `hosted_apis_not_in_plan` otherwise).

        Parameters
        ----------
        api_id : str
            Hosted API id (prefixed external id, `api_...`).

        consumer_key_id : str
            Consumer key id (prefixed external id, `ckey_...`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HostedApiKey
            OK

        Examples
        --------
        from speechify import Speechify

        client = Speechify(
            "2026-09-30",
            token="YOUR_TOKEN",
        )
        client.hosted_apis.keys.revoke_key(
            api_id="api_01jqr8x9zg5k2m3n4p5q6r7s8t",
            consumer_key_id="ckey_01jqr8x9zg5k2m3n4p5q6r7s8t",
        )
        """
        _response = self._raw_client.revoke_key(api_id, consumer_key_id, request_options=request_options)
        return _response.data


class AsyncKeysClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawKeysClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawKeysClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawKeysClient
        """
        return self._raw_client

    async def list_keys(
        self,
        api_id: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[HostedApiKey, ListHostedApiKeysResponse]:
        """
        List a hosted API's consumer keys, newest first, revoked ones included.
        Dark launch: requires the `hosted_apis_access` entitlement (402 `hosted_apis_not_in_plan` otherwise).

        Parameters
        ----------
        api_id : str
            Hosted API id (prefixed external id, `api_...`).

        cursor : typing.Optional[str]
            Opaque pagination cursor from a previous response.

        limit : typing.Optional[int]
            Max items per page (default 50, max 200).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[HostedApiKey, ListHostedApiKeysResponse]
            OK

        Examples
        --------
        import asyncio

        from speechify import AsyncSpeechify

        client = AsyncSpeechify(
            "2026-09-30",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.hosted_apis.keys.list_keys(
                api_id="api_01jqr8x9zg5k2m3n4p5q6r7s8t",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list_keys(api_id, cursor=cursor, limit=limit, request_options=request_options)

    async def create_key(
        self,
        api_id: str,
        *,
        name: str,
        idempotency_key: typing.Optional[str] = None,
        rate_per_minute: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HostedApiKey:
        """
        Mint a consumer key (`ck_...`) for the API's own callers. The plaintext
        `secret` is present in this response only; every later read shows the
        masked `key_hint`.
        Dark launch: requires the `hosted_apis_access` entitlement (402 `hosted_apis_not_in_plan` otherwise).

        Parameters
        ----------
        api_id : str
            Hosted API id (prefixed external id, `api_...`).

        name : str

        idempotency_key : typing.Optional[str]
            A client-generated key (an opaque string, max 255 chars) that makes a
            side-effect POST safe to retry: the server runs the operation exactly
            once and replays the first response (its status and body) for 24 hours.
            Reusing a key with a different request body, or while the first request
            is still in flight, returns `409 idempotency_conflict`. A replayed
            response carries the `Idempotent-Replayed: true` header.

        rate_per_minute : typing.Optional[int]
            Requests per minute; 60 when omitted, 0 for unlimited.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HostedApiKey
            The created key, with its one-time secret.

        Examples
        --------
        import asyncio

        from speechify import AsyncSpeechify

        client = AsyncSpeechify(
            "2026-09-30",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.hosted_apis.keys.create_key(
                api_id="api_01jqr8x9zg5k2m3n4p5q6r7s8t",
                idempotency_key="a1b2c3d4-5e6f-7a8b-9c0d-1e2f3a4b5c6d",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_key(
            api_id,
            name=name,
            idempotency_key=idempotency_key,
            rate_per_minute=rate_per_minute,
            request_options=request_options,
        )
        return _response.data

    async def revoke_key(
        self, api_id: str, consumer_key_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HostedApiKey:
        """
        Revoke a consumer key; idempotent. Requests carrying it are refused from now on.
        Dark launch: requires the `hosted_apis_access` entitlement (402 `hosted_apis_not_in_plan` otherwise).

        Parameters
        ----------
        api_id : str
            Hosted API id (prefixed external id, `api_...`).

        consumer_key_id : str
            Consumer key id (prefixed external id, `ckey_...`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HostedApiKey
            OK

        Examples
        --------
        import asyncio

        from speechify import AsyncSpeechify

        client = AsyncSpeechify(
            "2026-09-30",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.hosted_apis.keys.revoke_key(
                api_id="api_01jqr8x9zg5k2m3n4p5q6r7s8t",
                consumer_key_id="ckey_01jqr8x9zg5k2m3n4p5q6r7s8t",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.revoke_key(api_id, consumer_key_id, request_options=request_options)
        return _response.data
