# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class ConsentChallenge(UniversalBaseModel):
    id: str = pydantic.Field()
    """
    Identifier for this challenge, sent back as `consent_challenge_id`
    on the create. Treat it as an opaque string - the format is not part
    of the contract and will not stay stable.
    """

    phrase: str = pydantic.Field()
    """
    The sentence the speaker must read aloud. Show it exactly as
    returned - the recording is transcribed and matched against this
    text, so re-wording, re-casing or re-punctuating it will fail the
    check.
    """

    expires_at: dt.datetime = pydantic.Field()
    """
    When the challenge stops being usable. This is the only authority on
    the window - do not hard-code a duration. Past it, create a new
    challenge and record the new phrase.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
