# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .hosted_api_mount_operation import HostedApiMountOperation
from .hosted_api_mount_summary import HostedApiMountSummary
from .hosted_api_mount_tool_kind import HostedApiMountToolKind


class HostedApiMount(UniversalBaseModel):
    tool_id: str
    tool_name: str
    tool_kind: HostedApiMountToolKind
    name_prefix: str
    path_prefix: str
    dry_run: bool
    operations: typing.List[HostedApiMountOperation] = pydantic.Field()
    """
    One entry per operation the connector offers, in its order, then one per stale route whose operation it no longer offers.
    """

    summary: HostedApiMountSummary
    plan_digest: str = pydantic.Field()
    """
    Names this plan's content: every operation the connector offers and
    every route the API holds for it, whatever `operations` selects,
    and nothing that moves without the plan changing. Send it back as
    the apply's `plan_digest` to write only this plan.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
