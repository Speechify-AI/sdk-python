# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class RequestAnalyticsBucket(UniversalBaseModel):
    """
    One granularity bucket of the request analytics series.
    """

    time: dt.datetime = pydantic.Field()
    """
    Start of the bucket.
    """

    requests: int
    errors: int
    server_errors: int
    success_rate: float
    avg_latency_ms: float
    p50latency_ms: typing_extensions.Annotated[
        float, FieldMetadata(alias="p50_latency_ms"), pydantic.Field(alias="p50_latency_ms")
    ]
    p95latency_ms: typing_extensions.Annotated[
        float, FieldMetadata(alias="p95_latency_ms"), pydantic.Field(alias="p95_latency_ms")
    ]
    p99latency_ms: typing_extensions.Annotated[
        float, FieldMetadata(alias="p99_latency_ms"), pydantic.Field(alias="p99_latency_ms")
    ]
    ttfb_requests: int = pydantic.Field()
    """
    How many of `requests` were streaming synthesis (`POST
    /v1/audio/stream`) and so reported a time-to-first-byte. Every other
    route leaves the `p*_ttfb_ms` fields meaningless; when this is 0,
    render a gap rather than a zero.
    """

    p50ttfb_ms: typing_extensions.Annotated[
        float,
        FieldMetadata(alias="p50_ttfb_ms"),
        pydantic.Field(
            alias="p50_ttfb_ms", description="Median time to first byte over the bucket's streaming requests."
        ),
    ]
    p95ttfb_ms: typing_extensions.Annotated[
        float,
        FieldMetadata(alias="p95_ttfb_ms"),
        pydantic.Field(
            alias="p95_ttfb_ms",
            description="95th-percentile time to first byte over the bucket's streaming\nrequests. This is also time to first *audio*: the streaming response\nbody is raw audio, so the first byte is the first audio byte.\nScoped to streaming on purpose. `POST /v1/audio/speech` returns the\nwhole audio Base64-encoded in one JSON response, so its TTFB is its\ntotal latency (already reported as `p*_latency_ms`) and blending the\ntwo would describe neither.",
        ),
    ]
    p99ttfb_ms: typing_extensions.Annotated[
        float,
        FieldMetadata(alias="p99_ttfb_ms"),
        pydantic.Field(
            alias="p99_ttfb_ms", description="99th-percentile time to first byte over the bucket's streaming requests."
        ),
    ]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
